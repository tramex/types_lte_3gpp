# types_lte_3gpp

## Generate 3GPP types

```sh
python build.py

#compile only
python build.py --compile
```

## Notes

- some asn files should be manually patched (see `*_patch` functions in `build.py`)
